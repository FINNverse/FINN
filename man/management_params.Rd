% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/management.R
\name{management_params}
\alias{management_params}
\title{Management parameters (the common "genome")}
\usage{
management_params(
  target_diameter = Inf,
  target_harvest_fraction = 0,
  target_diameter_spread = 5,
  thinning_intensity = 0,
  thinning_size_bias = 0,
  species_removal_pref = 0,
  planting_rate = 0,
  entry_interval_years = 5,
  risk_level = 0
)
}
\arguments{
\item{target_diameter}{\verb{Zielstärke} (cm): the dbh at which a tree is harvested.
Scalar or a per-species vector. \code{Inf} (default) means no target-diameter
harvest.}

\item{target_harvest_fraction}{\verb{Nutzungsstärke} (0-1): share of at/above-target
stems removed per entry.}

\item{target_diameter_spread}{\verb{Zielstärken-Streubreite} (cm, > 0): how sharp
(small) vs. gradual (large) the target-diameter threshold is.}

\item{thinning_intensity}{\verb{Durchforstungsstärke} (0-1): mean fraction removed
per entry among the tended (sub-target) trees.}

\item{thinning_size_bias}{\code{Durchforstungsart}: which sizes the thinning takes.
\verb{< 0} from below (Niederdurchforstung), \verb{> 0} from above (Hochdurchforstung),
\code{0} neutral. Either a scalar OR a \verb{function(stand)} returning a scalar, where
\code{stand} is a list with \code{Dg} (quadratic mean dbh) and \code{target} (per-cohort
target diameter) -- use a function to let the bias swing from below to above
as the stand matures (the NW-FVA graduated regime; see \code{\link[=graduated_bias]{graduated_bias()}}).}

\item{species_removal_pref}{\code{Mischungsregulierung / Baumartenwahl}: scalar or
per-species. \code{0} neutral, \verb{> 0} removed preferentially (convert away, e.g.
spruce), \verb{< 0} retained/favoured. Enters as a factor \code{1 + pref}.}

\item{planting_rate}{\verb{Verjüngung / Vorbau} (0-1): advance-planting rate of
target species. Acts on regeneration, not removal (used by the driver).}

\item{entry_interval_years}{\code{Eingriffsturnus}: years between interventions.}

\item{risk_level}{\code{Risikostufe} (0-1): a single dial that lowers
\code{target_diameter} (by up to 15\% at \code{risk_level = 1}).}
}
\value{
An object of class \code{management_params}.
}
\description{
Builds one point in FINN's continuous management parameter space. Every
strategy -- thinning, target-diameter harvest, species conversion, and each of
the WET2024 forest development types -- is expressed as a value of these
parameters, never a discrete case. Names carry the WET vocabulary.
}
\seealso{
\code{\link[=apply_management]{apply_management()}}, \code{\link[=graduated_bias]{graduated_bias()}}, \code{\link[=thin_from_below]{thin_from_below()}},
\code{\link[=target_diameter_harvest]{target_diameter_harvest()}}
}
