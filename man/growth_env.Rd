% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Processes.R
\name{growth_env}
\alias{growth_env}
\title{Calculate growth with an environment-controlled size-decline}
\usage{
growth_env(
  dbh,
  species,
  parGrowth,
  pred,
  light,
  light_steepness = 10,
  debug = FALSE,
  trees = NULL
)
}
\arguments{
\item{dbh}{torch.Tensor Diameter at breast height.}

\item{species}{torch.Tensor species of tree.}

\item{parGrowth}{torch.Tensor Growth parameters.}

\item{pred}{torch.Tensor Predicted values.}

\item{light}{torch.Tensor Accumulated Light.}

\item{light_steepness}{numeric Steepness of the light-response sigmoid. Defaults to 10.}

\item{debug}{logical If TRUE, return the intermediate components as a list. Defaults to FALSE.}

\item{trees}{torch.Tensor (Optional) Number of trees per cohort. Defaults to NULL.}
}
\value{
torch.Tensor A tensor of relative diameter growth per timestep.
}
\description{
A variant of \code{\link[=growth]{growth()}} in which the rate at which diameter growth slows with
size is not a fixed per-species constant but responds to the environment. The
built-in \code{\link[=growth]{growth()}} uses \code{g = shade * exp(pred) * exp(-k * dbh)} with a
species-only decline \code{k = parGrowth[,2]}; here the decline is
\code{k_eff = k_base * exp(-k_env * pred)}, i.e. a \strong{species baseline} \code{k_base}
(still \code{parGrowth[,2]}) \strong{combined with the environment} through the same
growth predictor \code{pred}. A positive \code{k_env} lets productive environments
(higher \code{pred}) both grow faster \emph{and} sustain diameter growth longer (smaller
decline) — the pattern seen when fitting the NW-FVA yield tables, where a single
species \code{k} over- and under-shoots the best and poorest site classes.
}
\details{
\code{k_env} is supplied as a per-species (or scalar) trainable parameter through
\code{createProcess(custom_parameters = list(k_env = ...))}; it is read here as
\code{self$k_env}. With \code{k_env = 0} (or absent) this reduces \strong{exactly} to
\code{\link[=growth]{growth()}}, so it is a strict, opt-in extension.
}
\examples{
\dontrun{
m <- finn(
  N_species      = Nsp,
  growth_process = createProcess(~ temp + prec, FINN::growth_env,
                                 optimizeSpecies = TRUE, optimizeEnv = TRUE,
                                 custom_parameters = list(k_env = rep(0, Nsp))),
  ...
)
}
}
