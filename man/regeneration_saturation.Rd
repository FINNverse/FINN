% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Processes.R
\name{regeneration_saturation}
\alias{regeneration_saturation}
\title{Regeneration with Beverton-Holt density saturation}
\usage{
regeneration_saturation(species, parReg, pred, light, debug = FALSE)
}
\arguments{
\item{species}{torch.Tensor species information.}

\item{parReg}{torch.Tensor Regeneration parameters. 0 <= parReg <= 1
This parameter denotes the fraction of light needed for a species to regenerate.
In general low values for high regeneration and high values for low regeneration.}

\item{pred}{torch.Tensor Prediction values.}

\item{light}{torch.Tensor Available light variable for calculation.}

\item{debug}{logical If TRUE, return the intermediate components as a list. Defaults to FALSE.}
}
\value{
torch.Tensor Saturated regeneration values for forest patches.
}
\description{
A drop-in alternative to \link{regeneration} that caps the otherwise
unbounded mean recruitment with a Beverton-Holt density dependence,
\code{recruits = K * m / (K + m)}, where \code{m} is the mechanistic mean recruitment
(identical to \link{regeneration}) and \code{K} is a fitted carrying capacity
(stems ha^-1 step^-1). As \code{m} grows, recruitment saturates towards \code{K}.
}
\details{
Unlike the other process functions, whose species parameters come from the
built-in \code{par_regeneration} table, this function needs one extra parameter,
the log carrying capacity \code{reg_logK} (\code{K = exp(reg_logK)}). Declare it with
\code{createProcess(custom_parameters = list(reg_logK = ...))}; FINN then registers
it as a trainable parameter, optimises it in \code{\link[=fit]{fit()}} and round-trips it
through \code{torch_save}/\code{torch_load}. The \strong{length of the init decides the
resolution}: a length-1 init gives one shared \code{K} for all species; a
length-\code{N_species} init gives a per-species \code{K}. Read the fitted value back
with \code{exp(as.numeric(m$reg_logK))}.
}
\examples{
\dontrun{
m <- finn(
  N_species = Nsp,
  regeneration_process = createProcess(
    ~ temp + prec, FINN::regeneration_saturation,
    custom_parameters = list(reg_logK = rep(log(800), Nsp)), # per-species K
    optimizeSpecies = TRUE, optimizeEnv = TRUE)
)
}

}
\seealso{
\link{regeneration}, \code{\link[=createProcess]{createProcess()}}
}
