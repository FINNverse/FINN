% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{fia_init_trees}
\alias{fia_init_trees}
\alias{fia_init_test}
\title{Oregon FIA calibration data: initial tree list}
\format{
A \code{data.table} with one row per tree (6039 / 6083 rows) and 28
columns. The ones FINN uses:
\describe{
\item{\code{siteID}, \code{patchID}, \code{year}}{site and patch index; \code{year} is 0.}
\item{\code{species}, \code{species_name}}{species index \verb{1..11} and name, coded
as in \link{fia_species_dt}.}
\item{\code{dbh}}{diameter at breast height, cm.}
\item{\code{trees}}{trees represented by the record (\code{1}; \code{NA} for records
that are not living stems).}
\item{\code{treeName}}{unique tree identifier.}
}
The remaining columns (\code{siteName}, \code{patchName}, \code{OrigYear}, FIA's \code{SPCD}
and \code{STATUSCD}, \code{status}, \code{living}, and the per-tree change fields such as
\code{dbh_before}, \code{dbh_growth}, \code{mort}, \code{reg}) are carried over from the
remeasurement tree list produced by \code{\link[=makeObsData]{makeObsData()}}; at year 0 most of
them are \code{NA} by construction.

An object of class \code{data.table} (inherits from \code{data.frame}) with 6083 rows and 28 columns.
}
\source{
Subsampled by \code{dev/make_extdata.R} (\code{set.seed(42)}) from the Oregon
FIA plots prepared in the (private) \code{FINNverse/FINN-fia} analysis repo:
tree records via the FIA
database, climate from WorldClim bioclim variables attached per plot. The
full provenance chain is in \code{data-raw/README.md}.
}
\usage{
fia_init_trees

fia_init_test
}
\description{
The year-0 tree list of the \link{fia_obs_dt} training sites (\code{fia_init_trees})
and the \link{fia_obs_test} holdout sites (\code{fia_init_test}): what a site looked
like at the first census, from which \code{\link[=makeInitCohorts]{makeInitCohorts()}} builds the initial
cohorts a fit or simulation starts from
(\code{makeInitCohorts(fia_init_trees, Nspecies = 11)}).
}
\seealso{
Other fia datasets: 
\code{\link{fia_env_dt}},
\code{\link{fia_obs_dt}},
\code{\link{fia_species_dt}}
}
\concept{fia datasets}
\keyword{datasets}
