% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xAI.R
\name{conditionalEffects}
\alias{conditionalEffects}
\title{Conditional effects of a FINN model}
\usage{
conditionalEffects(
  model,
  env = NULL,
  init_cohort = NULL,
  env_autoscale = TRUE,
  sim_seed = 42L
)
}
\arguments{
\item{model}{(\code{finn_class})\cr fitted model.}

\item{env}{(\code{data.table|data.frame}|\code{NULL})\cr env covariates; \code{NULL} uses the cached training env.}

\item{init_cohort}{(\code{CohortMat}|\code{NULL})\cr initial cohort; \code{NULL} uses the cached training init_cohort.}

\item{env_autoscale}{(\code{logical(1)})\cr see \code{\link[=ALE]{ALE()}}.}

\item{sim_seed}{(\code{integer(1)})\cr seed for the state-harvesting simulation (see \code{\link[=ALE]{ALE()}}).}
}
\value{
an object of class \code{FINNconditionalEffects} (also cached on \code{model$conditional_effects}).
}
\description{
Computes (and caches on the model) the conditional effects — the local
derivatives of each demographic process (growth, mortality, regeneration)
with respect to its inputs, per process and per species. This is the shared
primitive behind \code{\link[=ALE]{ALE()}}, \code{\link[=averageConditionalEffects]{averageConditionalEffects()}} and the analytical
variable importance in \code{\link[=summary.finn_class]{summary.finn_class()}}.
}
