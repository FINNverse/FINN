% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xAI.R
\name{plot.FINNale}
\alias{plot.FINNale}
\title{Plot ALE curves of a FINN model}
\usage{
\method{plot}{FINNale}(x, process = NULL, scale = FALSE, ...)
}
\arguments{
\item{x}{an object of class \code{FINNale} (returned by \code{\link[=ALE]{ALE()}}).}

\item{process}{(\code{character}|\code{NULL})\cr restrict the plot to one process (\code{"growth"},
\code{"mortality"} or \code{"regeneration"}); \code{NULL} (default) plots all three.}

\item{scale}{(\code{logical(1)})\cr if \code{TRUE}, divide each curve by the SD of its
process x species rate so the effects are dimensionless and comparable (a shared
y-axis is then used); if \code{FALSE} (default) the raw ALE is shown with free y-axes.}

\item{...}{currently ignored.}
}
\value{
invisibly, the \code{ggplot} object.
}
\description{
Plots the accumulated local effect (ALE) curves produced by \code{\link[=ALE]{ALE()}} as a
grid: one row per demographic process (growth, mortality, regeneration) and
one column per environmental predictor. When several species are present,
their curves are overlaid in the same panel as one coloured line each.
}
