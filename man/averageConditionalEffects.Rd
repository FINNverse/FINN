% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xAI.R
\name{averageConditionalEffects}
\alias{averageConditionalEffects}
\title{Average conditional effects of a FINN model}
\usage{
averageConditionalEffects(
  model,
  env = NULL,
  init_cohort = NULL,
  env_autoscale = TRUE,
  sim_seed = 42L,
  env_only = TRUE
)
}
\arguments{
\item{model}{(\code{finn_class})\cr fitted model.}

\item{env}{(\code{data.table|data.frame}|\code{NULL})\cr env covariates; \code{NULL} uses the cached training env.}

\item{init_cohort}{(\code{CohortMat}|\code{NULL})\cr initial cohort; \code{NULL} uses the cached training init_cohort.}

\item{env_autoscale}{(\code{logical(1)})\cr see \code{\link[=ALE]{ALE()}}.}

\item{sim_seed}{(\code{integer(1)})\cr seed for the state-harvesting simulation (see \code{\link[=ALE]{ALE()}}).}

\item{env_only}{(\code{logical(1)})\cr if \code{TRUE} (default) report only the environmental
predictors of each process; if \code{FALSE} also include the stand-state inputs
(dbh, light, growth) each process depends on.}
}
\value{
a named list (one entry per process) of data.frames with columns
\verb{species, variable, mean_effect}.
}
\description{
Summarises the conditional effects into a per process x species x variable
average marginal effect: the mean local derivative (\code{mean_effect}, an
approximate linear effect). Derived cheaply from the cached conditional
effects.
}
