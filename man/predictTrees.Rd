% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finn-class.R
\name{predictTrees}
\alias{predictTrees}
\title{Predict growth and mortality for individual observed trees}
\usage{
predictTrees(
  model,
  tree_data,
  env,
  patches = 100L,
  patch_size = 0.1,
  device = c("cpu", "gpu")
)
}
\arguments{
\item{model}{(\code{finn_class})\cr A (fitted) model.}

\item{tree_data}{(\code{data.table|data.frame})\cr Observed trees: \code{siteID},
\code{year}, \code{patchID}, \code{species}, \code{dbh} (at the interval start) and
\code{period_length} (the interval, in timesteps).}

\item{env}{(\code{data.table|data.frame})\cr Environment, as for \link{fit}.}

\item{patches}{(\code{integer(1)})\cr Number of patches, as in the fit.}

\item{patch_size}{(\code{numeric(1)})\cr Patch size, as in the fit.}

\item{device}{(\code{character(1)})\cr \code{"cpu"} or \code{"gpu"}.}
}
\value{
One row per row of \code{tree_data}, in the same order (\code{.row} records the
original position), with \code{growth_pred} (mean annual relative growth over
the interval) and \code{mort_pred} (probability of dying during the interval).
Bind these onto \code{tree_data} directly rather than joining on
siteID/year/patchID, which would be many-to-many.
}
\description{
Predict growth and mortality for individual observed trees
}
\details{
The tree-level counterpart of \link{predict.finn_class}. Where \code{predict()}
simulates a stand forward and reports site-level states, this evaluates the
growth and mortality kernels on trees you observed: light is computed from
the observed stand, the kernels run on those trees at their observed
diameters, and the annual rates are accumulated over each site's
remeasurement interval — growth as the mean of the annual rates, mortality as
the complement of the product of annual survivals.

It is the same code path \code{fit(..., loss_aggregation = c(growth = "tree", mortality = "tree"))} scores, so a validation report cannot drift from the
objective it is validating.
}
