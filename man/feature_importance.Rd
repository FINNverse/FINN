% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xAI.R
\name{feature_importance}
\alias{feature_importance}
\title{Permutation feature importance for FINN demographic rates}
\usage{
feature_importance(
  model,
  env = NULL,
  init_cohort = NULL,
  nperm = 20L,
  method = c("rmse", "sobol"),
  seed = NULL,
  sim_seed = 42L,
  env_autoscale = TRUE,
  ...
)
}
\arguments{
\item{model}{(\code{finn_class})\cr fitted model.}

\item{env}{(\code{data.table|data.frame}|\code{NULL})\cr env covariates; \code{NULL} uses the cached training env.}

\item{init_cohort}{(\code{CohortMat}|\code{NULL})\cr init cohort; \code{NULL} uses the cached training init_cohort.}

\item{nperm}{(\code{integer(1)})\cr number of permutation replicates (default 20).}

\item{method}{(\code{character(1)})\cr \code{"rmse"} or \code{"sobol"}.}

\item{seed}{(\code{integer(1)}|\code{NULL})\cr R RNG seed controlling which permutations are drawn.}

\item{sim_seed}{(\code{integer(1)}|\code{NULL})\cr torch seed for common random numbers across runs.}

\item{env_autoscale}{(\code{logical(1)})\cr see \code{\link[=ALE]{ALE()}}. \code{TRUE} (default) leaves \code{predict()} to
rescale raw \code{env} internally; \code{FALSE} treats \code{env} as already on the model scale.}

\item{...}{passed to \code{\link[=predict]{predict()}} (e.g. \code{patches}, \code{patch_size}).}
}
\value{
a named list (one per process) of data.frames with columns
\verb{species, variable, importance}, sorted within species.
}
\description{
Permutes each environmental predictor of a process and measures how strongly
the permutation shifts that process's predicted rate, \strong{per process and per
species}. Unlike the analytical ALE-variance importance, this re-simulates
the model, so it captures the full dynamical response (feedback through the
stand state) rather than the process response function alone. It does NOT use
the conditional-effects cache.
}
\details{
Two scorings via \code{method}:
\itemize{
\item \code{"rmse"} — RMSE between the unpermuted and permuted rate, in units of that
species' rate SD. Unbounded; larger = more important.
\item \code{"sobol"} — total-effect estimator \code{0.5 * mean(MSE_shift) / Var(rate)};
dimensionless (only bounded in \verb{[0, 1]} under independent predictors — FINN's
climate predictors are usually correlated, so treat it as relative).
}

Predictors are read per-process from the model formulas, so processes with
different formulas get different variable sets. Common random numbers
(\code{sim_seed}, applied to the torch simulation RNG) make the stochastic
mortality/regeneration draws shared across the reference and permuted runs,
so a driver with no effect returns ~0.
}
