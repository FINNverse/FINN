% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Processes.R
\name{growth_poly}
\alias{growth_poly}
\title{Calculate growth with a flexible polynomial size response}
\usage{
growth_poly(
  dbh,
  species,
  parGrowth,
  pred,
  light,
  light_steepness = 10,
  debug = FALSE,
  trees = NULL
)
}
\arguments{
\item{dbh}{torch.Tensor Diameter at breast height.}

\item{species}{torch.Tensor species of tree.}

\item{parGrowth}{torch.Tensor Growth parameters.}

\item{pred}{torch.Tensor Predicted values.}

\item{light}{torch.Tensor Accumulated Light.}

\item{light_steepness}{numeric Steepness of the light-response sigmoid. Defaults to 10.}

\item{debug}{logical If TRUE, return the intermediate components as a list. Defaults to FALSE.}

\item{trees}{torch.Tensor (Optional) Number of trees per cohort. Defaults to NULL.}
}
\value{
torch.Tensor A tensor of relative diameter growth per timestep.
}
\description{
A growth process whose dependence on diameter is an arbitrary-degree polynomial
rather than the fixed exponential decline of \code{\link[=growth]{growth()}}. Where \code{\link[=growth]{growth()}} uses
\code{g = shade * exp(pred) * exp(-k * dbh)} (a single decline rate \code{k}),
\code{growth_poly()} uses
\deqn{g = shade * exp\\big(pred + \\sum_{d=1}^{D} a_d\\,(dbh/s)^d\\big),}
a polynomial of degree \code{D} in the (scaled) diameter inside the exponent, so the
growth-vs-size curve can bend to whatever shape the data require — useful when a
single exponential cannot reproduce a yield-table trajectory exactly.
}
\details{
The coefficients are supplied through
\code{createProcess(custom_parameters = list(poly_coef = rep(0, D)))} and read here as
\code{self$poly_coef}; the degree is simply the length of that vector, and \code{fit()}
estimates them. Diameter is scaled by \code{poly_scale} (default 50 cm, read from
\code{self$poly_scale} if set) to keep the powers well-conditioned. With
\code{poly_coef = c(-k*s)} (degree 1) this is exactly \code{\link[=growth]{growth()}}; with no \code{poly_coef}
it falls back to the mechanistic linear decline \code{parGrowth[,2]}.

For a fit whose \emph{shape} should differ by site, either combine this with an
environmental term in \code{pred} (the level), use \code{\link[=growth_env]{growth_env()}} (an
environment-controlled decline), or replace growth with a neural network via
\code{\link[=createHybrid]{createHybrid()}}.
}
\examples{
\dontrun{
m <- finn(N_species = Nsp,
  growth_process = createProcess(~ env1, FINN::growth_poly,
                                 optimizeSpecies = TRUE, optimizeEnv = TRUE,
                                 custom_parameters = list(poly_coef = rep(0, 4))))
}
}
