% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Processes.R
\name{regeneration_adult}
\alias{regeneration_adult}
\title{Regeneration limited by conspecific adult density}
\usage{
regeneration_adult(
  species,
  parReg,
  pred,
  light,
  dbh = NULL,
  trees = NULL,
  debug = FALSE
)
}
\arguments{
\item{species}{torch.Tensor species information.}

\item{parReg}{torch.Tensor Regeneration parameters. 0 <= parReg <= 1
This parameter denotes the fraction of light needed for a species to regenerate.
In general low values for high regeneration and high values for low regeneration.}

\item{pred}{torch.Tensor Prediction values.}

\item{light}{torch.Tensor Available light variable for calculation.}

\item{dbh}{torch.Tensor cohort diameters, \verb{[sites, patches, cohorts]}.}

\item{trees}{torch.Tensor stems per cohort, \verb{[sites, patches, cohorts]}.}

\item{debug}{logical If TRUE, return the intermediate components as a list. Defaults to FALSE.}
}
\value{
torch.Tensor mean recruitment, stems ha^-1 step^-1,
\verb{[sites, patches, species]}. With \code{debug = TRUE}, a list that also holds
\code{adults} (A, stems ha^-1, \verb{[sites, 1, species]}) and \code{seed} (f(A)).
}
\description{
A mechanistic extension of \link{regeneration_saturation} in which
recruitment needs a seed source: the mean recruitment of a species scales
with the density of its own ADULTS on the site, the stems whose diameter
exceeds an adult threshold \code{x}.
}
\details{
For species \code{k} on a site,
\deqn{A_k = \frac{1}{P a} \sum_{\mathrm{cohorts\ of\ } k} n \, \sigma\!\left(\frac{d - x_k}{\tau}\right)}{A_k = sum(n * sigmoid((d - x_k)/tau)) / (P * a)}
is the conspecific adult density (stems ha^-1), pooled over the \code{P} patches
of the site (area \code{a} each) because seeds disperse beyond one patch. The soft
threshold (\code{tau} = 2 dbh units) keeps \code{x_k} differentiable; a hard \code{d > x}
would give it no gradient. The seed-limitation term
\deqn{f(A_k) = \frac{A_k}{A_k + A_{1/2,k}}}{f(A_k) = A_k / (A_k + A_half_k)}
is 0 without adults and approaches 1 once adults are abundant, so the mean
recruitment
\deqn{m = r(\mathrm{light}) \, e^{\mathrm{env}} f(A) + \mathrm{reg\_floor}}{m = regP(light) * exp(env) * f(A) + reg_floor}
falls to the floor (read it as immigration from outside the site) where the
species has no adult, and rises to the light- and environment-driven
potential of \link{regeneration} where adults are many. The result is capped
with the same Beverton-Holt term as \link{regeneration_saturation},
\verb{K m / (K + m)}.

Declare three extra parameters with \code{createProcess(custom_parameters = )}.
As for \code{reg_logK}, a length-1 init gives one shared value and a
length-\code{N_species} init one value per species:
\itemize{
\item \code{reg_logK}: the log Beverton-Holt cap, stems ha^-1 step^-1;
\item \code{reg_adult_logx}: the log adult threshold, \code{x = exp(reg_adult_logx)} in the
units of \code{dbh}. Once \code{x} falls below the smallest diameter present, every
stem counts as an adult and \code{x} stops receiving gradient;
\item \code{reg_adult_logA}: the log half-saturation adult density,
\code{A_half = exp(reg_adult_logA)} stems ha^-1.
}

\code{forward()} passes \code{dbh} and \code{trees} to a regeneration function only when its
signature has them, so the other regeneration functions are unaffected.
Called without \code{dbh}/\code{trees} (as the ALE code in \code{xAI.R} does), the function
returns recruitment with the seed source present (\code{f = 1}).
}
\examples{
\dontrun{
m <- finn(
  N_species = Nsp,
  regeneration_process = createProcess(
    ~ temp + prec, FINN::regeneration_adult,
    custom_parameters = list(reg_logK       = rep(log(50), Nsp),
                             reg_adult_logx = rep(log(20), Nsp),   # adult from 20 cm
                             reg_adult_logA = rep(log(20), Nsp)),  # f = 0.5 at 20 adults/ha
    optimizeSpecies = TRUE, optimizeEnv = TRUE)
)
}

}
\seealso{
\link{regeneration_saturation}, \code{\link[=createProcess]{createProcess()}}
}
