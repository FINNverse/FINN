# dev/max — Max's scratch

Personal experiments, pilots, and working scripts. Nothing here ships in the
package (`dev/` is in `.Rbuildignore`). Keep your own scratch under this folder so
it never collides with Yannek's or leaks into a package/release commit. Promote
anything reusable into `R/`, `tests/`, or shared `dev/` tooling via a PR.
